name: Gradle Build e Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout sources
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'

    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v4

    - name: Make gradlew executable
      run: chmod +x gradlew

    - name: Build with Gradle
      run: ./gradlew build --build-cache --parallel

    - name: Collect and sanitize artifacts
      run: |
        mkdir -p release-artifacts
        
        VERSION=${GITHUB_REF#refs/tags/v}
        
        copy_clean_jar() {
          local folder=$1
          local loader_name=$2
          
          for jar in $folder/build/libs/*.jar; do
            if [[ -f "$jar" && ! "$jar" =~ -(dev|sources|plain|javadoc)\.jar$ ]]; then
              cp "$jar" "release-artifacts/LucidAdvancements ${loader_name} ${VERSION}.jar"
            fi
          done
        }
        
        copy_clean_jar "fabric" "Fabric"
        copy_clean_jar "forge" "Forge"
        copy_clean_jar "neoforge" "NeoForge"
        
        ls -lh release-artifacts/

    - name: Generate Release Notes
      run: |
        CURRENT_TAG=${GITHUB_REF#refs/tags/}
        PREVIOUS_TAG=$(git describe --tags --abbrev=0 HEAD^ 2>/dev/null || echo "")
        
        echo "## 📝 What's changed?" > changelog.txt
        echo "" >> changelog.txt
        
        if [ -z "$PREVIOUS_TAG" ]; then
          git log --no-merges --pretty=format:"* %s" >> changelog.txt
        else
          git log ${PREVIOUS_TAG}..HEAD --no-merges --pretty=format:"* %s" >> changelog.txt
        fi

    - name: Create Release and Upload Asset
      uses: softprops/action-gh-release@v2
      with:
        files: release-artifacts/*.jar
        body_path: changelog.txt
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}